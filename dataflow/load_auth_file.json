{
  "name" : "load_auth_file",
  "type" : "Microsoft.DataFactory/factories/dataflows",
  "apiVersion" : "2018-06-01",
  "properties" : {
    "type" : "MappingDataFlow",
    "folder" : {
<<<<<<< HEAD
      "name" : "NonWellsfargo_1108/PACKAGES"
=======
      "name" : "Elevance/PACKAGES"
>>>>>>> f82d097b0fe4e20ea258fb75f3c2f0740d60809f
    },
    "typeProperties" : {
      "sources" : [ {
        "name" : "FlatFileSource",
        "dataset" : {
          "referenceName" : "Generic_File_SRC",
          "type" : "DatasetReference"
        }
      } ],
      "sinks" : [ {
        "dataset" : {
          "referenceName" : "Generic_File_TGT",
          "type" : "DatasetReference"
        },
        "name" : "OutputPlaceHolderOracleDestination"
      } ],
      "transformations" : [ {
        "name" : "DerivedColumn"
      }, {
        "name" : "DerivedColumn1"
      } ],
<<<<<<< HEAD
      "script" : "parameters{\n\tArchiveFilePath as string (),\n\tAuthFileName as string (),\n\tAuthOBFileName as string (),\n\tAuthRecCount as integer (),\n\tAuthTermFileName as string (),\n\tAuthTermRecCount as integer (),\n\tConfFileName as string (),\n\tConfOBFileName as string (),\n\tConfOBQuery as string (),\n\tConfRecCount as integer (),\n\tConfTermFileName as string (),\n\tConfTermRecCount as integer (),\n\tInboundFileLocation as string (),\n\tPWD as string (),\n\tResRecCount as integer (),\n\tResTermRecCount as integer (),\n\tRestFileName as string (),\n\tRestOBFileName as string (),\n\tRestOBQuery as string (),\n\tRestTermFileName as string (),\n\tRUNID as string (),\n\tServer as string (),\n\tToFolder as string (),\n\tUserName as string ()\n}\n\nsource(output(\n\t\tEnd as string,\n\t\tN_FIRST as string,\n\t\tN_MID_INIT as string,\n\t\tN_LAST as string,\n\t\tN_SUFFIX as string,\n\t\tI_SSN as string,\n\t\tD_DOB as string,\n\t\tC_HIPAA_GENDER as string,\n\t\tF_EMPLOYEE as string,\n\t\tC_PREFIX as string,\n\t\tI_CONTRACT as string,\n\t\tI_HCID as string,\n\t\tC_INDIV_REL as string,\n\t\tC_ANT_ENT as string,\n\t\tC_AUTH_TYPE as string,\n\t\tC_AUTH_DOC_TYPE as string,\n\t\tD_AUTH_BEGIN as string,\n\t\tD_AUTH_END as string,\n\t\tN_SIGNED_FIRST as string,\n\t\tN_SIGNED_LAST as string,\n\t\tN_TITLE as string,\n\t\tN_AUTH_FIRST as string,\n\t\tN_AUTH_MID as string,\n\t\tN_AUTH_LAST as string,\n\t\tN_AUTH_SUFFIX as string,\n\t\tC_COUNTRY_CODE_HOME as string,\n\t\tC_PHONE_AREA_HOME as string,\n\t\tT_PHONE_HOME as string,\n\t\tC_PHONE_EXT_HOME as string,\n\t\tC_COUNTRY_CODE_WORK as string,\n\t\tC_PHONE_AREA_WORK as string,\n\t\tT_PHONE_WORK as string,\n\t\tC_PHONE_EXT_WORK as string,\n\t\tC_COUNTRY_CODE_CELL as string,\n\t\tC_PHONE_AREA_CELL as string,\n\t\tT_PHONE_CELL as string,\n\t\tC_PHONE_EXT_CELL as string,\n\t\tC_COUNTRY_CODE_INT as string,\n\t\tC_PHONE_AREA_INT as string,\n\t\tT_PHONE_INT as string,\n\t\tC_PHONE_EXT_INT as string,\n\t\tT_DETAILS as string,\n\t\tF_NOTES as string\n\t),\n\tallowSchemaDrift: true,\n\tvalidateSchema: false) ~> FlatFileSource\nFlatFileSource derive(RunId = toDouble(RUNID),\n\t\tFileType = toString(trim('AUTH')),\n\t\tDOB = iif(isNull(D_DOB) || length(trim(D_DOB)) == 0, toTimestamp(toString('1900-01-01', 'MM/dd/yyyy'), 'MM/dd/yyyy'), toTimestamp(toString((substring(toString(D_DOB), toInteger(1), toInteger(4)) + '-' + substring(toString(D_DOB), toInteger(5), toInteger(2)) + '-' + substring(toString(D_DOB), toInteger(7), toInteger(2))), 'MM/dd/yyyy'), 'MM/dd/yyyy')),\n\t\tAuthEffectiveDate = iif(isNull(D_AUTH_BEGIN) || length(trim(D_AUTH_BEGIN)) == 0, toTimestamp(toString('1900-01-01', 'MM/dd/yyyy'), 'MM/dd/yyyy'), toTimestamp(toString((substring(toString(D_AUTH_BEGIN), toInteger(1), toInteger(4)) + '-' + substring(toString(D_AUTH_BEGIN), toInteger(5), toInteger(2)) + '-' + substring(toString(D_AUTH_BEGIN), toInteger(7), toInteger(2))), 'MM/dd/yyyy'), 'MM/dd/yyyy')),\n\t\tAuthExpirationDate = iif(isNull(D_AUTH_END) || length(trim(D_AUTH_END)) == 0, toTimestamp(toString('1900-01-01', 'MM/dd/yyyy'), 'MM/dd/yyyy'), toTimestamp(toString((substring(toString(D_AUTH_END), toInteger(1), toInteger(4)) + '-' + substring(toString(D_AUTH_END), toInteger(5), toInteger(2)) + '-' + substring(toString(D_AUTH_END), toInteger(7), toInteger(2))), 'MM/dd/yyyy'), 'MM/dd/yyyy'))) ~> DerivedColumn\nDerivedColumn derive(AuthExpirationDate = toTimestamp(toString(iif((greaterOrEqual(AuthExpirationDate,toTimestamp(toString('2099/12/31', 'MM/dd/yyyy'), 'MM/dd/yyyy'))), toDate(toString('2099-12-31', 'MM/dd/yyyy'), \"MM/dd/yyyy\"), toDate(toString(AuthExpirationDate, 'MM/dd/yyyy'), \"MM/dd/yyyy\")), 'MM/dd/yyyy'), 'MM/dd/yyyy')) ~> DerivedColumn1\nDerivedColumn1 sink(input(\n\t\tAuthExpirationDate as timestamp 'dd-MM-yyyy',\n\t\tRunId as double,\n\t\tFileType as string,\n\t\tDOB as timestamp 'ect.format.datetime.hypen.4year2month2day2hour24mmnute2secondsdot6nanoseconds',\n\t\tAuthEffectiveDate as timestamp 'ect.format.datetime.hypen.4year2month2day2hour24mmnute2secondsdot6nanoseconds',\n\t\tEnd as string,\n\t\tN_FIRST as string,\n\t\tN_MID_INIT as string,\n\t\tN_LAST as string,\n\t\tN_SUFFIX as string,\n\t\tI_SSN as string,\n\t\tD_DOB as string,\n\t\tC_HIPAA_GENDER as string,\n\t\tF_EMPLOYEE as string,\n\t\tC_PREFIX as string,\n\t\tI_CONTRACT as string,\n\t\tI_HCID as string,\n\t\tC_INDIV_REL as string,\n\t\tC_ANT_ENT as string,\n\t\tC_AUTH_TYPE as string,\n\t\tC_AUTH_DOC_TYPE as string,\n\t\tD_AUTH_BEGIN as string,\n\t\tD_AUTH_END as string,\n\t\tN_SIGNED_FIRST as string,\n\t\tN_SIGNED_LAST as string,\n\t\tN_TITLE as string,\n\t\tN_AUTH_FIRST as string,\n\t\tN_AUTH_MID as string,\n\t\tN_AUTH_LAST as string,\n\t\tN_AUTH_SUFFIX as string,\n\t\tC_COUNTRY_CODE_HOME as string,\n\t\tC_PHONE_AREA_HOME as string,\n\t\tT_PHONE_HOME as string,\n\t\tC_PHONE_EXT_HOME as string,\n\t\tC_COUNTRY_CODE_WORK as string,\n\t\tC_PHONE_AREA_WORK as string,\n\t\tT_PHONE_WORK as string,\n\t\tC_PHONE_EXT_WORK as string,\n\t\tC_COUNTRY_CODE_CELL as string,\n\t\tC_PHONE_AREA_CELL as string,\n\t\tT_PHONE_CELL as string,\n\t\tC_PHONE_EXT_CELL as string,\n\t\tC_COUNTRY_CODE_INT as string,\n\t\tC_PHONE_AREA_INT as string,\n\t\tT_PHONE_INT as string,\n\t\tC_PHONE_EXT_INT as string,\n\t\tT_DETAILS as string,\n\t\tF_NOTES as string\n\t),\n\tallowSchemaDrift: true,\n\tvalidateSchema: false,\n\tmapColumn(\n\t\tAuthExpirationDate,\n\t\tRunId,\n\t\tFileType,\n\t\tDOB,\n\t\tAuthEffectiveDate,\n\t\tEnd,\n\t\tN_FIRST,\n\t\tN_MID_INIT,\n\t\tN_LAST,\n\t\tN_SUFFIX,\n\t\tI_SSN,\n\t\tD_DOB,\n\t\tC_HIPAA_GENDER,\n\t\tF_EMPLOYEE,\n\t\tC_PREFIX,\n\t\tI_CONTRACT,\n\t\tI_HCID,\n\t\tC_INDIV_REL,\n\t\tC_ANT_ENT,\n\t\tC_AUTH_TYPE,\n\t\tC_AUTH_DOC_TYPE,\n\t\tD_AUTH_BEGIN,\n\t\tD_AUTH_END,\n\t\tN_SIGNED_FIRST,\n\t\tN_SIGNED_LAST,\n\t\tN_TITLE,\n\t\tN_AUTH_FIRST,\n\t\tN_AUTH_MID,\n\t\tN_AUTH_LAST,\n\t\tN_AUTH_SUFFIX,\n\t\tC_COUNTRY_CODE_HOME,\n\t\tC_PHONE_AREA_HOME,\n\t\tT_PHONE_HOME,\n\t\tC_PHONE_EXT_HOME,\n\t\tC_COUNTRY_CODE_WORK,\n\t\tC_PHONE_AREA_WORK,\n\t\tT_PHONE_WORK,\n\t\tC_PHONE_EXT_WORK,\n\t\tC_COUNTRY_CODE_CELL,\n\t\tC_PHONE_AREA_CELL,\n\t\tT_PHONE_CELL,\n\t\tC_PHONE_EXT_CELL,\n\t\tC_COUNTRY_CODE_INT,\n\t\tC_PHONE_AREA_INT,\n\t\tT_PHONE_INT,\n\t\tC_PHONE_EXT_INT,\n\t\tT_DETAILS,\n\t\tF_NOTES)) ~> OutputPlaceHolderOracleDestination"
=======
      "script" : "parameters{\n\tArchiveFilePath as string (),\n\tAuthFileName as string (),\n\tAuthOBFileName as string (),\n\tAuthRecCount as integer (),\n\tAuthTermFileName as string (),\n\tAuthTermRecCount as integer (),\n\tConfFileName as string (),\n\tConfOBFileName as string (),\n\tConfOBQuery as string (),\n\tConfRecCount as integer (),\n\tConfTermFileName as string (),\n\tConfTermRecCount as integer (),\n\tInboundFileLocation as string (),\n\tPWD as string (),\n\tResRecCount as integer (),\n\tResTermRecCount as integer (),\n\tRestFileName as string (),\n\tRestOBFileName as string (),\n\tRestOBQuery as string (),\n\tRestTermFileName as string (),\n\tRUNID as string (),\n\tServer as string (),\n\tToFolder as string (),\n\tUserName as string ()\n}\n\nsource(output(\n\t\tEnd as string,\n\t\tN_FIRST as string,\n\t\tN_MID_INIT as string,\n\t\tN_LAST as string,\n\t\tN_SUFFIX as string,\n\t\tI_SSN as string,\n\t\tD_DOB as string,\n\t\tC_HIPAA_GENDER as string,\n\t\tF_EMPLOYEE as string,\n\t\tC_PREFIX as string,\n\t\tI_CONTRACT as string,\n\t\tI_HCID as string,\n\t\tC_INDIV_REL as string,\n\t\tC_ANT_ENT as string,\n\t\tC_AUTH_TYPE as string,\n\t\tC_AUTH_DOC_TYPE as string,\n\t\tD_AUTH_BEGIN as string,\n\t\tD_AUTH_END as string,\n\t\tN_SIGNED_FIRST as string,\n\t\tN_SIGNED_LAST as string,\n\t\tN_TITLE as string,\n\t\tN_AUTH_FIRST as string,\n\t\tN_AUTH_MID as string,\n\t\tN_AUTH_LAST as string,\n\t\tN_AUTH_SUFFIX as string,\n\t\tC_COUNTRY_CODE_HOME as string,\n\t\tC_PHONE_AREA_HOME as string,\n\t\tT_PHONE_HOME as string,\n\t\tC_PHONE_EXT_HOME as string,\n\t\tC_COUNTRY_CODE_WORK as string,\n\t\tC_PHONE_AREA_WORK as string,\n\t\tT_PHONE_WORK as string,\n\t\tC_PHONE_EXT_WORK as string,\n\t\tC_COUNTRY_CODE_CELL as string,\n\t\tC_PHONE_AREA_CELL as string,\n\t\tT_PHONE_CELL as string,\n\t\tC_PHONE_EXT_CELL as string,\n\t\tC_COUNTRY_CODE_INT as string,\n\t\tC_PHONE_AREA_INT as string,\n\t\tT_PHONE_INT as string,\n\t\tC_PHONE_EXT_INT as string,\n\t\tT_DETAILS as string,\n\t\tF_NOTES as string\n\t),\n\tallowSchemaDrift: true,\n\tvalidateSchema: false) ~> FlatFileSource\nFlatFileSource derive(RunId = toDouble($RUNID),\n\t\tFileType = toString(trim('AUTH')),\n\t\tDOB = iif(isNull(D_DOB) || length(trim(D_DOB)) == 0, toTimestamp(toString('1900-01-01', 'MM/dd/yyyy'), 'MM/dd/yyyy'), toTimestamp(toString((substring(toString(D_DOB), toInteger(1), toInteger(4)) + '-' + substring(toString(D_DOB), toInteger(5), toInteger(2)) + '-' + substring(toString(D_DOB), toInteger(7), toInteger(2))), 'MM/dd/yyyy'), 'MM/dd/yyyy')),\n\t\tAuthEffectiveDate = iif(isNull(D_AUTH_BEGIN) || length(trim(D_AUTH_BEGIN)) == 0, toTimestamp(toString('1900-01-01', 'MM/dd/yyyy'), 'MM/dd/yyyy'), toTimestamp(toString((substring(toString(D_AUTH_BEGIN), toInteger(1), toInteger(4)) + '-' + substring(toString(D_AUTH_BEGIN), toInteger(5), toInteger(2)) + '-' + substring(toString(D_AUTH_BEGIN), toInteger(7), toInteger(2))), 'MM/dd/yyyy'), 'MM/dd/yyyy')),\n\t\tAuthExpirationDate = iif(isNull(D_AUTH_END) || length(trim(D_AUTH_END)) == 0, toTimestamp(toString('1900-01-01', 'MM/dd/yyyy'), 'MM/dd/yyyy'), toTimestamp(toString((substring(toString(D_AUTH_END), toInteger(1), toInteger(4)) + '-' + substring(toString(D_AUTH_END), toInteger(5), toInteger(2)) + '-' + substring(toString(D_AUTH_END), toInteger(7), toInteger(2))), 'MM/dd/yyyy'), 'MM/dd/yyyy'))) ~> DerivedColumn\nDerivedColumn derive(AuthExpirationDate = toTimestamp(toString(iif((greaterOrEqual(AuthExpirationDate,toTimestamp(toString('2099/12/31', 'MM/dd/yyyy'), 'MM/dd/yyyy'))), toDate(toString('2099-12-31', 'MM/dd/yyyy'), \"MM/dd/yyyy\"), toDate(toString(AuthExpirationDate, 'MM/dd/yyyy'), \"MM/dd/yyyy\")), 'MM/dd/yyyy'), 'MM/dd/yyyy')) ~> DerivedColumn1\nDerivedColumn1 sink(input(\n\t\tAuthExpirationDate as timestamp 'dd-MM-yyyy',\n\t\tRunId as double,\n\t\tFileType as string,\n\t\tDOB as timestamp 'ect.format.datetime.hypen.4year2month2day2hour24mmnute2secondsdot6nanoseconds',\n\t\tAuthEffectiveDate as timestamp 'ect.format.datetime.hypen.4year2month2day2hour24mmnute2secondsdot6nanoseconds',\n\t\tEnd as string,\n\t\tN_FIRST as string,\n\t\tN_MID_INIT as string,\n\t\tN_LAST as string,\n\t\tN_SUFFIX as string,\n\t\tI_SSN as string,\n\t\tD_DOB as string,\n\t\tC_HIPAA_GENDER as string,\n\t\tF_EMPLOYEE as string,\n\t\tC_PREFIX as string,\n\t\tI_CONTRACT as string,\n\t\tI_HCID as string,\n\t\tC_INDIV_REL as string,\n\t\tC_ANT_ENT as string,\n\t\tC_AUTH_TYPE as string,\n\t\tC_AUTH_DOC_TYPE as string,\n\t\tD_AUTH_BEGIN as string,\n\t\tD_AUTH_END as string,\n\t\tN_SIGNED_FIRST as string,\n\t\tN_SIGNED_LAST as string,\n\t\tN_TITLE as string,\n\t\tN_AUTH_FIRST as string,\n\t\tN_AUTH_MID as string,\n\t\tN_AUTH_LAST as string,\n\t\tN_AUTH_SUFFIX as string,\n\t\tC_COUNTRY_CODE_HOME as string,\n\t\tC_PHONE_AREA_HOME as string,\n\t\tT_PHONE_HOME as string,\n\t\tC_PHONE_EXT_HOME as string,\n\t\tC_COUNTRY_CODE_WORK as string,\n\t\tC_PHONE_AREA_WORK as string,\n\t\tT_PHONE_WORK as string,\n\t\tC_PHONE_EXT_WORK as string,\n\t\tC_COUNTRY_CODE_CELL as string,\n\t\tC_PHONE_AREA_CELL as string,\n\t\tT_PHONE_CELL as string,\n\t\tC_PHONE_EXT_CELL as string,\n\t\tC_COUNTRY_CODE_INT as string,\n\t\tC_PHONE_AREA_INT as string,\n\t\tT_PHONE_INT as string,\n\t\tC_PHONE_EXT_INT as string,\n\t\tT_DETAILS as string,\n\t\tF_NOTES as string\n\t),\n\tallowSchemaDrift: true,\n\tvalidateSchema: false,\n\tmapColumn(\n\t\tAuthExpirationDate,\n\t\tRunId,\n\t\tFileType,\n\t\tDOB,\n\t\tAuthEffectiveDate,\n\t\tEnd,\n\t\tN_FIRST,\n\t\tN_MID_INIT,\n\t\tN_LAST,\n\t\tN_SUFFIX,\n\t\tI_SSN,\n\t\tD_DOB,\n\t\tC_HIPAA_GENDER,\n\t\tF_EMPLOYEE,\n\t\tC_PREFIX,\n\t\tI_CONTRACT,\n\t\tI_HCID,\n\t\tC_INDIV_REL,\n\t\tC_ANT_ENT,\n\t\tC_AUTH_TYPE,\n\t\tC_AUTH_DOC_TYPE,\n\t\tD_AUTH_BEGIN,\n\t\tD_AUTH_END,\n\t\tN_SIGNED_FIRST,\n\t\tN_SIGNED_LAST,\n\t\tN_TITLE,\n\t\tN_AUTH_FIRST,\n\t\tN_AUTH_MID,\n\t\tN_AUTH_LAST,\n\t\tN_AUTH_SUFFIX,\n\t\tC_COUNTRY_CODE_HOME,\n\t\tC_PHONE_AREA_HOME,\n\t\tT_PHONE_HOME,\n\t\tC_PHONE_EXT_HOME,\n\t\tC_COUNTRY_CODE_WORK,\n\t\tC_PHONE_AREA_WORK,\n\t\tT_PHONE_WORK,\n\t\tC_PHONE_EXT_WORK,\n\t\tC_COUNTRY_CODE_CELL,\n\t\tC_PHONE_AREA_CELL,\n\t\tT_PHONE_CELL,\n\t\tC_PHONE_EXT_CELL,\n\t\tC_COUNTRY_CODE_INT,\n\t\tC_PHONE_AREA_INT,\n\t\tT_PHONE_INT,\n\t\tC_PHONE_EXT_INT,\n\t\tT_DETAILS,\n\t\tF_NOTES)) ~> OutputPlaceHolderOracleDestination"
>>>>>>> f82d097b0fe4e20ea258fb75f3c2f0740d60809f
    }
  }
}