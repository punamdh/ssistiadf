{
  "name" : "data_flow_task_2",
  "type" : "Microsoft.DataFactory/factories/dataflows",
  "apiVersion" : "2018-06-01",
  "properties" : {
    "type" : "MappingDataFlow",
    "folder" : {
      "name" : "NonWellsfargo_1108/PACKAGES"
    },
    "typeProperties" : {
      "sources" : [ {
        "name" : "ReadFalloutMembers",
        "dataset" : {
          "referenceName" : "Excel1",
          "type" : "DatasetReference"
        }
      } ],
      "sinks" : [ {
        "dataset" : {
          "referenceName" : "genric_parquet",
          "type" : "DatasetReference"
        },
        "name" : "OLEDBDestination"
      } ],
      "transformations" : [ {
        "name" : "DataConversion"
      } ],
      "script" : "parameters{\n\tArchivePath as string (),\n\tBatchDate as string (),\n\tBatchId as integer (),\n\tDateFormat as string (),\n\tDateTimeFinal as string (),\n\tEndDate as string (),\n\tEndTime as string (),\n\tErrorFileName as string (),\n\tErrorFilePath as string (),\n\tErrorRecordCount as integer (),\n\texEndDateTime as string (),\n\texStartDateTime as string (),\n\tFalloutPath as string (),\n\tGroupID as string (),\n\tGroupName as string (),\n\tHeader as string (),\n\tIsAdhocDailyRun as string (),\n\tIsFallOutReport as string (),\n\tIsMultiRunMode as string (),\n\tIsOneTimeProcess as string (),\n\tJobDate as string (),\n\tobjOneTimeGroups as string (),\n\tOneTimeReverseSync as string (),\n\tOracleDataSource as string (),\n\tOraclePassword as string (),\n\tOracleProvider as string (),\n\tOracleUserId as string (),\n\tOutputFileName as string (),\n\tOutputPath as string (),\n\tQueryGroup as string (),\n\tReverseSyncQuery as string (),\n\tReverseSyncQueryDaily as string (),\n\tReverseSyncQuery_Range as string (),\n\tSQLCatalog as string (),\n\tSQLDataSource as string (),\n\tSQLPassword as string (),\n\tSQLProvider as string (),\n\tSQLUserName as string (),\n\tStartDate as string (),\n\tStartTime as string (),\n\tUpdateEndTime as string ()\n}\n\nsource(output(\n\t\tGroup as string,\n\t\tSubscriber as string\n\t),\n\tallowSchemaDrift: true,\n\tvalidateSchema: false,\n\tignoreNoFilesFound: false) ~> ReadFalloutMembers\nReadFalloutMembers derive({Copy of Subscriber} = toString(Subscriber),\n\t\t{Copy of Group} = toString(Group)) ~> DataConversion\nDataConversion sink(input(\n\t\tSubscriberID as string,\n\t\tBatchID as integer,\n\t\tGroupID as string\n\t),\n\tallowSchemaDrift: true,\n\tvalidateSchema: false,\n\tstagingSchemaName: 'dbo',\n\tsaveOrder: 10000,\n\tinsertable:true,\n\tdeletable:false,\n\tupsertable:false,\n\tupdateable:false,\n\tkeys:[],\n\tmapColumn(\n\t\tSubscriberID = {Copy of Subscriber},\n\t\tBatchID = BatchId,\n\t\tGroupID = {Copy of Group})) ~> OLEDBDestination"
    }
  }
}