{
  "name" : "load_rest_term_file_1",
  "type" : "Microsoft.DataFactory/factories/dataflows",
  "apiVersion" : "2018-06-01",
  "properties" : {
    "type" : "MappingDataFlow",
    "folder" : {
      "name" : "NonWellsfargo_1108/PACKAGES"
    },
    "typeProperties" : {
      "sources" : [ {
        "name" : "FlatFileSource",
        "dataset" : {
          "referenceName" : "Generic_File_SRC",
          "type" : "DatasetReference"
        }
      } ],
      "sinks" : [ {
        "dataset" : {
          "referenceName" : "genric_parquet",
          "type" : "DatasetReference"
        },
        "name" : "OLEDBDestination"
      } ],
      "transformations" : [ {
        "name" : "DerivedColumn"
      }, {
        "name" : "PlaceHolderRowCount"
      } ],
      "script" : "parameters{\n\tArchiveFilePath as string (),\n\tAuthFileName as string (),\n\tAuthOBFileName as string (),\n\tAuthRecCount as integer (),\n\tAuthTermFileName as string (),\n\tAuthTermRecCount as integer (),\n\tConfFileName as string (),\n\tConfOBFileName as string (),\n\tConfOBQuery as string (),\n\tConfRecCount as integer (),\n\tConfTermFileName as string (),\n\tConfTermRecCount as integer (),\n\tInboundFileLocation as string (),\n\tPWD as string (),\n\tResRecCount as integer (),\n\tResTermRecCount as integer (),\n\tRestFileName as string (),\n\tRestOBFileName as string (),\n\tRestOBQuery as string (),\n\tRestTermFileName as string (),\n\tRUNID as string (),\n\tServer as string (),\n\tToFolder as string (),\n\tUserName as string ()\n}\n\nsource(output(\n\t\tN_FIRST as string,\n\t\tN_MID_INIT as string,\n\t\tN_LAST as string,\n\t\tN_SUFFIX as string,\n\t\tI_SSN as string,\n\t\tD_DOB as string,\n\t\tC_HIPAA_GENDER as string,\n\t\tF_EMPLOYEE as string,\n\t\tC_PREFIX as string,\n\t\tI_CONTRACT as string,\n\t\tI_HCID as string,\n\t\tC_INDIV_REL as string,\n\t\tC_ANT_ENT as string,\n\t\tEnd as string,\n\t\tN_FIRST_P as string,\n\t\tN_MID_INIT_P as string,\n\t\tN_LAST_P as string,\n\t\tN_SUFFIX_P as string,\n\t\tF_HONOR_RESTRICT as string,\n\t\tC_RESTRICTION as string,\n\t\tN_FIRST_RESTRICT as string,\n\t\tN_LAST_RESTRICT as string,\n\t\tD_COMPLETED as string,\n\t\tN_FIRST_COMPLETED as string,\n\t\tN_LAST_COMPLETED as string,\n\t\tC_NOTIFY_METH as string,\n\t\tD_NOTIFIED as string,\n\t\tN_FIRST_NOTIFIED as string,\n\t\tN_LAST_NOTIFIED as string,\n\t\tD_EFFECT_RESTRICT as string,\n\t\tD_CANCEL_RESTRICT as string,\n\t\tT_RESTRICT_DESCRIP as string,\n\t\tC_REQUEST_REL as string,\n\t\tC_REQUESTOR_TYPE as string,\n\t\tC_REQUEST_METHOD as string,\n\t\tD_RECEIVED as string,\n\t\tFILLER as string\n\t),\n\tallowSchemaDrift: true,\n\tvalidateSchema: false) ~> FlatFileSource\nFlatFileSource derive(RunID = toInteger(RUNID),\n\t\tFileType = toString(trim('REST_TERM')),\n\t\tD_DOB = iif(isNull(D_DOB) || length(trim(D_DOB)) == 0, toDate(toString('1900-01-01', 'MM/dd/yyyy'), \"MM/dd/yyyy\"), toDate(toString((substring(toString(D_DOB), toInteger(1), toInteger(4)) + '-' + substring(toString(D_DOB), toInteger(5), toInteger(2)) + '-' + substring(toString(D_DOB), toInteger(7), toInteger(2))), 'MM/dd/yyyy'), \"MM/dd/yyyy\")),\n\t\tD_COMPLETED = iif(isNull(D_COMPLETED) || length(trim(D_COMPLETED)) == 0, toDate(toString('1900-01-01', 'MM/dd/yyyy'), \"MM/dd/yyyy\"), toDate(toString((substring(toString(D_COMPLETED), toInteger(1), toInteger(4)) + '-' + substring(toString(D_COMPLETED), toInteger(5), toInteger(2)) + '-' + substring(toString(D_COMPLETED), toInteger(7), toInteger(2))), 'MM/dd/yyyy'), \"MM/dd/yyyy\")),\n\t\tD_NOTIFIED = iif(isNull(D_NOTIFIED) || length(trim(D_NOTIFIED)) == 0, toDate(toString('1900-01-01', 'MM/dd/yyyy'), \"MM/dd/yyyy\"), toDate(toString((substring(toString(D_NOTIFIED), toInteger(1), toInteger(4)) + '-' + substring(toString(D_NOTIFIED), toInteger(5), toInteger(2)) + '-' + substring(toString(D_NOTIFIED), toInteger(7), toInteger(2))), 'MM/dd/yyyy'), \"MM/dd/yyyy\")),\n\t\tD_EFFECT_RESTRICT = iif(isNull(D_EFFECT_RESTRICT) || length(trim(D_EFFECT_RESTRICT)) == 0, toDate(toString('1900-01-01', 'MM/dd/yyyy'), \"MM/dd/yyyy\"), toDate(toString((substring(toString(D_EFFECT_RESTRICT), toInteger(1), toInteger(4)) + '-' + substring(toString(D_EFFECT_RESTRICT), toInteger(5), toInteger(2)) + '-' + substring(toString(D_EFFECT_RESTRICT), toInteger(7), toInteger(2))), 'MM/dd/yyyy'), \"MM/dd/yyyy\")),\n\t\tD_CANCEL_RESTRICT = iif(isNull(D_CANCEL_RESTRICT) || length(trim(D_CANCEL_RESTRICT)) == 0, toDate(toString('1900-01-01', 'MM/dd/yyyy'), \"MM/dd/yyyy\"), toDate(toString((substring(toString(D_CANCEL_RESTRICT), toInteger(1), toInteger(4)) + '-' + substring(toString(D_CANCEL_RESTRICT), toInteger(5), toInteger(2)) + '-' + substring(toString(D_CANCEL_RESTRICT), toInteger(7), toInteger(2))), 'MM/dd/yyyy'), \"MM/dd/yyyy\")),\n\t\tD_RECEIVED = iif(isNull(D_RECEIVED) || length(trim(D_RECEIVED)) == 0, toDate(toString('1900-01-01', 'MM/dd/yyyy'), \"MM/dd/yyyy\"), toDate(toString((substring(toString(D_RECEIVED), toInteger(1), toInteger(4)) + '-' + substring(toString(D_RECEIVED), toInteger(5), toInteger(2)) + '-' + substring(toString(D_RECEIVED), toInteger(7), toInteger(2))), 'MM/dd/yyyy'), \"MM/dd/yyyy\"))) ~> DerivedColumn\nDerivedColumn derive(RunID = RunID) ~> PlaceHolderRowCount\nPlaceHolderRowCount sink(input(\n\t\tD_EFFECT_RESTRICT as timestamp 'dd-MM-yyyy',\n\t\tN_LAST_NOTIFIED as string,\n\t\tC_ANT_ENT as string,\n\t\tN_LAST_COMPLETED as string,\n\t\tF_EMPLOYEE as string,\n\t\tC_REQUEST_REL as string,\n\t\tC_PREFIX as string,\n\t\tI_SSN as string,\n\t\tD_COMPLETED as timestamp 'dd-MM-yyyy',\n\t\tC_HIPAA_GENDER as string,\n\t\tT_RESTRICT_DESCRIP as string,\n\t\tN_LAST_P as string,\n\t\tC_RESTRICTION as string,\n\t\tC_REQUESTOR_TYPE as string,\n\t\tN_MID_INIT as string,\n\t\tN_SUFFIX_P as string,\n\t\tN_FIRST_RESTRICT as string,\n\t\tN_SUFFIX as string,\n\t\tN_LAST as string,\n\t\tN_FIRST as string,\n\t\tI_HCID as string,\n\t\tD_RECEIVED as timestamp 'dd-MM-yyyy',\n\t\tN_FIRST_P as string,\n\t\tFILETYPE as string,\n\t\tFILLER as string,\n\t\tRUNID as string,\n\t\tC_NOTIFY_METH as string,\n\t\tC_REQUEST_METHOD as string,\n\t\tN_LAST_RESTRICT as string,\n\t\tN_MID_INIT_P as string,\n\t\tD_CANCEL_RESTRICT as timestamp 'dd-MM-yyyy',\n\t\tC_INDIV_REL as string,\n\t\tD_DOB as timestamp 'dd-MM-yyyy',\n\t\tN_FIRST_NOTIFIED as string,\n\t\tD_NOTIFIED as timestamp 'dd-MM-yyyy',\n\t\tI_CONTRACT as string,\n\t\tF_HONOR_RESTRICT as string,\n\t\tN_FIRST_COMPLETED as string\n\t),\n\tallowSchemaDrift: true,\n\tvalidateSchema: false,\n\tstagingSchemaName: 'dbo',\n\tsaveOrder: 10000,\n\tinsertable:true,\n\tdeletable:false,\n\tupsertable:false,\n\tupdateable:false,\n\tkeys:[],\n\tmapColumn(\n\t\tD_EFFECT_RESTRICT,\n\t\tN_LAST_NOTIFIED,\n\t\tC_ANT_ENT,\n\t\tN_LAST_COMPLETED,\n\t\tF_EMPLOYEE,\n\t\tC_REQUEST_REL,\n\t\tC_PREFIX,\n\t\tI_SSN,\n\t\tD_COMPLETED,\n\t\tC_HIPAA_GENDER,\n\t\tT_RESTRICT_DESCRIP,\n\t\tN_LAST_P,\n\t\tC_RESTRICTION,\n\t\tC_REQUESTOR_TYPE,\n\t\tN_MID_INIT,\n\t\tN_SUFFIX_P,\n\t\tN_FIRST_RESTRICT,\n\t\tN_SUFFIX,\n\t\tN_LAST,\n\t\tN_FIRST,\n\t\tI_HCID,\n\t\tD_RECEIVED,\n\t\tN_FIRST_P,\n\t\tFILETYPE = FileType,\n\t\tFILLER,\n\t\tRUNID = RunID,\n\t\tC_NOTIFY_METH,\n\t\tC_REQUEST_METHOD,\n\t\tN_LAST_RESTRICT,\n\t\tN_MID_INIT_P,\n\t\tD_CANCEL_RESTRICT,\n\t\tC_INDIV_REL,\n\t\tD_DOB,\n\t\tN_FIRST_NOTIFIED,\n\t\tD_NOTIFIED,\n\t\tI_CONTRACT,\n\t\tF_HONOR_RESTRICT,\n\t\tN_FIRST_COMPLETED)) ~> OLEDBDestination"
    }
  }
}