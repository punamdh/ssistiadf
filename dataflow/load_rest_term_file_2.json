{
  "name" : "load_rest_term_file_2",
  "type" : "Microsoft.DataFactory/factories/dataflows",
  "apiVersion" : "2018-06-01",
  "properties" : {
    "type" : "MappingDataFlow",
    "folder" : {
      "name" : "NonWellsfargo_1108/PACKAGES"
    },
    "typeProperties" : {
      "sources" : [ {
        "name" : "FlatFileSource",
        "dataset" : {
          "referenceName" : "Generic_File_SRC",
          "type" : "DatasetReference"
        }
      } ],
      "sinks" : [ {
        "dataset" : {
          "referenceName" : "genric_parquet",
          "type" : "DatasetReference"
        },
        "name" : "OLEDBDestination"
      } ],
      "transformations" : [ {
        "name" : "DerivedColumn"
      }, {
        "name" : "PlaceHolderRowCount"
      } ],
      "script" : "parameters{\n\tArchiveFilePath as string (),\n\tAuthFileName as string (),\n\tAuthOBFileName as string (),\n\tAuthRecCount as integer (),\n\tAuthTermFileName as string (),\n\tAuthTermRecCount as integer (),\n\tConfFileName as string (),\n\tConfOBFileName as string (),\n\tConfOBQuery as string (),\n\tConfRecCount as integer (),\n\tConfTermFileName as string (),\n\tConfTermRecCount as integer (),\n\tInboundFileLocation as string (),\n\tPWD as string (),\n\tResRecCount as integer (),\n\tResTermRecCount as integer (),\n\tRestFileName as string (),\n\tRestOBFileName as string (),\n\tRestOBQuery as string (),\n\tRestTermFileName as string (),\n\tRUNID as string (),\n\tServer as string (),\n\tToFolder as string (),\n\tUserName as string ()\n}\n\nsource(output(\n\t\tN_FIRST as string,\n\t\tN_MID_INIT as string,\n\t\tN_LAST as string,\n\t\tN_SUFFIX as string,\n\t\tI_SSN as string,\n\t\tD_DOB as string,\n\t\tC_HIPAA_GENDER as string,\n\t\tF_EMPLOYEE as string,\n\t\tC_PREFIX as string,\n\t\tI_CONTRACT as string,\n\t\tI_HCID as string,\n\t\tC_INDIV_REL as string,\n\t\tC_ANT_ENT as string,\n\t\tD_EFFECT_CONFCOMM as string,\n\t\tD_CANCEL_CONFCOMM as string,\n\t\tT_ADDRESS_LINE_1 as string,\n\t\tT_ADDRESS_LINE_2 as string,\n\t\tT_ADDRESS_LINE_3 as string,\n\t\tT_ADDRESS_CITY as string,\n\t\tC_STATE as string,\n\t\tC_ZIP as string,\n\t\tC_COUNTRY as string,\n\t\tC_COUNTRY_CODE_HOME as string,\n\t\tC_PHONE_AREA_HOME as string,\n\t\tT_PHONE_HOME as string,\n\t\tC_PHONE_EXT_HOME as string,\n\t\tC_COUNTRY_CODE_WORK as string,\n\t\tC_PHONE_AREA_WORK as string,\n\t\tT_PHONE_WORK as string,\n\t\tC_PHONE_EXT_WORK as string,\n\t\tC_COUNTRY_CODE_CELL as string,\n\t\tC_PHONE_AREA_CELL as string,\n\t\tT_PHONE_CELL as string,\n\t\tC_PHONE_EXT_CELL as string,\n\t\tC_COUNTRY_CODE_INTL as string,\n\t\tC_PHONE_AREA_INTL as string,\n\t\tT_PHONE_INTL as string,\n\t\tC_PHONE_EXT_INTL as string,\n\t\tT_PASSWORD as string,\n\t\tT_PASS_HINT as string,\n\t\tF_NOTES as string,\n\t\tMEM_EMAIL as string,\n\t\tEnd as string\n\t),\n\tallowSchemaDrift: true,\n\tvalidateSchema: false) ~> FlatFileSource\nFlatFileSource derive(RunID = toInteger(RUNID),\n\t\tFileType = toString(trim('CONF_TERM')),\n\t\tD_DOB = iif(isNull(D_DOB) || length(trim(D_DOB)) == 0, toDate(toString('1900-01-01', 'MM/dd/yyyy'), \"MM/dd/yyyy\"), toDate(toString((substring(toString(D_DOB), toInteger(1), toInteger(4)) + '-' + substring(toString(D_DOB), toInteger(5), toInteger(2)) + '-' + substring(toString(D_DOB), toInteger(7), toInteger(2))), 'MM/dd/yyyy'), \"MM/dd/yyyy\")),\n\t\tD_EFFECT_CONFCOMM = iif(isNull(D_EFFECT_CONFCOMM) || length(trim(D_EFFECT_CONFCOMM)) == 0, toDate(toString('1900-01-01', 'MM/dd/yyyy'), \"MM/dd/yyyy\"), toDate(toString((substring(toString(D_EFFECT_CONFCOMM), toInteger(1), toInteger(4)) + '-' + substring(toString(D_EFFECT_CONFCOMM), toInteger(5), toInteger(2)) + '-' + substring(toString(D_EFFECT_CONFCOMM), toInteger(7), toInteger(2))), 'MM/dd/yyyy'), \"MM/dd/yyyy\")),\n\t\tD_CANCEL_CONFCOMM = iif(isNull(D_CANCEL_CONFCOMM) || length(trim(D_CANCEL_CONFCOMM)) == 0, toDate(toString('1900-01-01', 'MM/dd/yyyy'), \"MM/dd/yyyy\"), toDate(toString((substring(toString(D_CANCEL_CONFCOMM), toInteger(1), toInteger(4)) + '-' + substring(toString(D_CANCEL_CONFCOMM), toInteger(5), toInteger(2)) + '-' + substring(toString(D_CANCEL_CONFCOMM), toInteger(7), toInteger(2))), 'MM/dd/yyyy'), \"MM/dd/yyyy\"))) ~> DerivedColumn\nDerivedColumn derive(RunID = RunID) ~> PlaceHolderRowCount\nPlaceHolderRowCount sink(input(\n\t\tC_COUNTRY_CODE_CELL as string,\n\t\tT_ADDRESS_CITY as string,\n\t\tC_ANT_ENT as string,\n\t\tF_EMPLOYEE as string,\n\t\tD_EFFECT_CONFCOMM as timestamp 'dd-MM-yyyy',\n\t\tC_PREFIX as string,\n\t\tI_SSN as string,\n\t\tT_PASSWORD as string,\n\t\tC_HIPAA_GENDER as string,\n\t\tC_PHONE_AREA_WORK as string,\n\t\tN_MID_INIT as string,\n\t\tC_PHONE_EXT_INTL as string,\n\t\tC_PHONE_EXT_HOME as string,\n\t\tD_CANCEL_CONFCOMM as timestamp 'dd-MM-yyyy',\n\t\tC_PHONE_EXT_CELL as string,\n\t\tC_COUNTRY_CODE_INTL as string,\n\t\tC_ZIP as string,\n\t\tC_COUNTRY_CODE_HOME as string,\n\t\tN_SUFFIX as string,\n\t\tN_LAST as string,\n\t\tC_PHONE_EXT_WORK as string,\n\t\tN_FIRST as string,\n\t\tT_ADDRESS_LINE_3 as string,\n\t\tT_PHONE_INTL as string,\n\t\tC_COUNTRY as string,\n\t\tMEM_EMAIL as string,\n\t\tI_HCID as string,\n\t\tT_ADDRESS_LINE_1 as string,\n\t\tT_PHONE_HOME as string,\n\t\tT_ADDRESS_LINE_2 as string,\n\t\tC_STATE as string,\n\t\tC_COUNTRY_CODE_WORK as string,\n\t\tT_PHONE_CELL as string,\n\t\tF_NOTES as string,\n\t\tFILETYPE as string,\n\t\tRUNID as string,\n\t\tC_PHONE_AREA_INTL as string,\n\t\tT_PHONE_WORK as string,\n\t\tT_PASS_HINT as string,\n\t\tC_INDIV_REL as string,\n\t\tD_DOB as timestamp 'dd-MM-yyyy',\n\t\tC_PHONE_AREA_HOME as string,\n\t\tC_PHONE_AREA_CELL as string,\n\t\tI_CONTRACT as string\n\t),\n\tallowSchemaDrift: true,\n\tvalidateSchema: false,\n\tstagingSchemaName: 'dbo',\n\tsaveOrder: 10000,\n\tinsertable:true,\n\tdeletable:false,\n\tupsertable:false,\n\tupdateable:false,\n\tkeys:[],\n\tmapColumn(\n\t\tC_COUNTRY_CODE_CELL,\n\t\tT_ADDRESS_CITY,\n\t\tC_ANT_ENT,\n\t\tF_EMPLOYEE,\n\t\tD_EFFECT_CONFCOMM,\n\t\tC_PREFIX,\n\t\tI_SSN,\n\t\tT_PASSWORD,\n\t\tC_HIPAA_GENDER,\n\t\tC_PHONE_AREA_WORK,\n\t\tN_MID_INIT,\n\t\tC_PHONE_EXT_INTL,\n\t\tC_PHONE_EXT_HOME,\n\t\tD_CANCEL_CONFCOMM,\n\t\tC_PHONE_EXT_CELL,\n\t\tC_COUNTRY_CODE_INTL,\n\t\tC_ZIP,\n\t\tC_COUNTRY_CODE_HOME,\n\t\tN_SUFFIX,\n\t\tN_LAST,\n\t\tC_PHONE_EXT_WORK,\n\t\tN_FIRST,\n\t\tT_ADDRESS_LINE_3,\n\t\tT_PHONE_INTL,\n\t\tC_COUNTRY,\n\t\tMEM_EMAIL,\n\t\tI_HCID,\n\t\tT_ADDRESS_LINE_1,\n\t\tT_PHONE_HOME,\n\t\tT_ADDRESS_LINE_2,\n\t\tC_STATE,\n\t\tC_COUNTRY_CODE_WORK,\n\t\tT_PHONE_CELL,\n\t\tF_NOTES,\n\t\tFILETYPE = FileType,\n\t\tRUNID = RunID,\n\t\tC_PHONE_AREA_INTL,\n\t\tT_PHONE_WORK,\n\t\tT_PASS_HINT,\n\t\tC_INDIV_REL,\n\t\tD_DOB,\n\t\tC_PHONE_AREA_HOME,\n\t\tC_PHONE_AREA_CELL,\n\t\tI_CONTRACT)) ~> OLEDBDestination"
    }
  }
}