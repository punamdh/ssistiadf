{
  "name" : "auth_ob_file_1",
  "type" : "Microsoft.DataFactory/factories/dataflows",
  "apiVersion" : "2018-06-01",
  "properties" : {
    "type" : "MappingDataFlow",
    "folder" : {
      "name" : "Elevance/PACKAGES"
    },
    "typeProperties" : {
      "sources" : [ {
        "name" : "OLEDBSource",
        "dataset" : {
          "referenceName" : "Generic_OLEDB_DS_SRC",
          "type" : "DatasetReference"
        }
      } ],
      "sinks" : [ {
        "dataset" : {
          "referenceName" : "Generic_File_TGT",
          "type" : "DatasetReference"
        },
        "name" : "FlatFileDestination"
      } ],
      "transformations" : [ {
        "name" : "DerivedColumn"
      }, {
        "name" : "DerivedColumn11"
      } ],
      "script" : "parameters{\n\tArchiveFilePath as string (),\n\tAuthFileName as string (),\n\tAuthOBFileName as string (),\n\tAuthRecCount as integer (),\n\tAuthTermFileName as string (),\n\tAuthTermRecCount as integer (),\n\tConfFileName as string (),\n\tConfOBFileName as string (),\n\tConfOBQuery as string (),\n\tConfRecCount as integer (),\n\tConfTermFileName as string (),\n\tConfTermRecCount as integer (),\n\tInboundFileLocation as string (),\n\tPWD as string (),\n\tResRecCount as integer (),\n\tResTermRecCount as integer (),\n\tRestFileName as string (),\n\tRestOBFileName as string (),\n\tRestOBQuery as string (),\n\tRestTermFileName as string (),\n\tRUNID as string (),\n\tServer as string (),\n\tToFolder as string (),\n\tUserName as string ()\n}\n\nsource(output(\n\t\tRUNID as string,\n\t\tFILETYPE as string,\n\t\tERROR_CODE as string,\n\t\tN_FIRST as string,\n\t\tN_MID_INIT as string,\n\t\tN_LAST as string,\n\t\tN_SUFFIX as string,\n\t\tI_SSN as string,\n\t\tD_DOB as timestamp,\n\t\tC_HIPAA_GENDER as string,\n\t\tF_EMPLOYEE as string,\n\t\tC_PREFIX as string,\n\t\tI_CONTRACT as string,\n\t\tI_HCID as string,\n\t\tC_INDIV_REL as string,\n\t\tC_ANT_ENT as string,\n\t\tC_AUTH_TYPE as string,\n\t\tC_AUTH_DOC_TYPE as string,\n\t\tD_AUTH_BEGIN as timestamp,\n\t\tD_AUTH_END as timestamp,\n\t\tN_SIGNED_FIRST as string,\n\t\tN_SIGNED_LAST as string,\n\t\tN_TITLE as string,\n\t\tN_AUTH_FIRST as string,\n\t\tN_AUTH_MID as string,\n\t\tN_AUTH_LAST as string,\n\t\tN_AUTH_SUFFIX as string,\n\t\tC_COUNTRY_CODE_HOME as string,\n\t\tC_PHONE_AREA_HOME as string,\n\t\tT_PHONE_HOME as string,\n\t\tC_PHONE_EXT_HOME as string,\n\t\tC_COUNTRY_CODE_WORK as string,\n\t\tC_PHONE_AREA_WORK as string,\n\t\tT_PHONE_WORK as string,\n\t\tC_PHONE_EXT_WORK as string,\n\t\tC_COUNTRY_CODE_CELL as string,\n\t\tC_PHONE_AREA_CELL as string,\n\t\tT_PHONE_CELL as string,\n\t\tC_PHONE_EXT_CELL as string,\n\t\tC_COUNTRY_CODE_INT as string,\n\t\tC_PHONE_AREA_INT as string,\n\t\tT_PHONE_INT as string,\n\t\tC_PHONE_EXT_INT as string,\n\t\tT_DETAILS as string,\n\t\tF_NOTES as string\n\t),\n\tallowSchemaDrift: true,\n\tvalidateSchema: false,\n\tisolationLevel: 'READ_UNCOMMITTED',\n\tquery: 'User::AuthOBQuery',\n\tformat: 'query',\n\tstaged:false) ~> OLEDBSource\nOLEDBSource derive(D_DOB = toString(year(D_DOB)) + right('0' + toString(month(D_DOB)), 2) + right('0' + toString(dayOfMonth(D_DOB)), 2),\n\t\tD_AUTH_BEGIN = toString(year(D_AUTH_BEGIN)) + right('0' + toString(month(D_AUTH_BEGIN)), 2) + right('0' + toString(dayOfMonth(D_AUTH_BEGIN)), 2),\n\t\tD_AUTH_END = toString(year(D_AUTH_END)) + right('0' + toString(month(D_AUTH_END)), 2) + right('0' + toString(dayOfMonth(D_AUTH_END)), 2),\n\t\tAuth_Last_Name = trim(substring(toString(trim(iifNull(N_AUTH_LAST,'')) + ' ' + trim(iifNull(N_AUTH_SUFFIX,''))), toInteger(1), toInteger(35))),\n\t\tT_PHONE_HOME = replace(T_PHONE_HOME, toString('-'), toString('')),\n\t\tT_PHONE_WORK = replace(T_PHONE_WORK, toString('-'), toString('')),\n\t\tT_PHONE_INT = replace(T_PHONE_INT, toString('-'), toString('')),\n\t\tT_PHONE_CELL = replace(T_PHONE_CELL, toString('-'), toString(''))) ~> DerivedColumn\nDerivedColumn derive(D_AUTH_END = iif((D_AUTH_END) == '19000101', '', D_AUTH_END),\n\t\tD_AUTH_BEGIN = iif((D_AUTH_BEGIN) == '19000101', '', D_AUTH_BEGIN),\n\t\tD_DOB = iif((D_DOB) == '19000101', '', D_DOB),\n\t\tC_AUTH_DOC_TYPE = (iif(C_AUTH_DOC_TYPE == 'FPOA', 'FPA', C_AUTH_DOC_TYPE)),\n\t\tC_AUTH_TYPE = (iif(C_AUTH_TYPE == 'FPOA', 'FPA', C_AUTH_TYPE))) ~> DerivedColumn11\nDerivedColumn11 sink(input(\n\t\tC_COUNTRY_CODE_CELL as string,\n\t\tD_AUTH_END as string,\n\t\tC_COUNTRY_CODE_INT as string,\n\t\tN_TITLE as string,\n\t\tC_ANT_ENT as string,\n\t\tF_EMPLOYEE as string,\n\t\tN_AUTH_LAST as string,\n\t\tC_PREFIX as string,\n\t\tI_SSN as string,\n\t\tC_HIPAA_GENDER as string,\n\t\tC_PHONE_AREA_WORK as string,\n\t\tN_SIGNED_FIRST as string,\n\t\tT_PHONE_INT as string,\n\t\tN_SIGNED_LAST as string,\n\t\tN_AUTH_FIRST as string,\n\t\tN_MID_INIT as string,\n\t\tC_PHONE_EXT_HOME as string,\n\t\tC_PHONE_EXT_CELL as string,\n\t\tC_AUTH_DOC_TYPE as string,\n\t\tC_COUNTRY_CODE_HOME as string,\n\t\tN_SUFFIX as string,\n\t\tN_LAST as string,\n\t\tC_PHONE_EXT_WORK as string,\n\t\tT_DETAILS as string,\n\t\tN_FIRST as string,\n\t\tN_AUTH_SUFFIX as string,\n\t\tI_HCID as string,\n\t\tT_PHONE_HOME as string,\n\t\tD_AUTH_BEGIN as string,\n\t\tC_COUNTRY_CODE_WORK as string,\n\t\tT_PHONE_CELL as string,\n\t\tF_NOTES as string,\n\t\tT_PHONE_WORK as string,\n\t\tC_INDIV_REL as string,\n\t\tD_DOB as string,\n\t\tC_PHONE_AREA_HOME as string,\n\t\tC_PHONE_AREA_CELL as string,\n\t\tC_PHONE_AREA_INT as string,\n\t\tC_PHONE_EXT_INT as string,\n\t\tI_CONTRACT as string,\n\t\tC_AUTH_TYPE as string,\n\t\tN_AUTH_MID as string\n\t),\n\tallowSchemaDrift: true,\n\tvalidateSchema: false,\n\tsaveOrder: 10000,\n\tmapColumn(\n\t\tC_COUNTRY_CODE_CELL,\n\t\tD_AUTH_END,\n\t\tC_COUNTRY_CODE_INT,\n\t\tN_TITLE,\n\t\tC_ANT_ENT,\n\t\tF_EMPLOYEE,\n\t\tN_AUTH_LAST = Auth_Last_Name,\n\t\tC_PREFIX,\n\t\tI_SSN,\n\t\tC_HIPAA_GENDER,\n\t\tC_PHONE_AREA_WORK,\n\t\tN_SIGNED_FIRST,\n\t\tT_PHONE_INT,\n\t\tN_SIGNED_LAST,\n\t\tN_AUTH_FIRST,\n\t\tN_MID_INIT,\n\t\tC_PHONE_EXT_HOME,\n\t\tC_PHONE_EXT_CELL,\n\t\tC_AUTH_DOC_TYPE,\n\t\tC_COUNTRY_CODE_HOME,\n\t\tN_SUFFIX,\n\t\tN_LAST,\n\t\tC_PHONE_EXT_WORK,\n\t\tT_DETAILS,\n\t\tN_FIRST,\n\t\tN_AUTH_SUFFIX,\n\t\tI_HCID,\n\t\tT_PHONE_HOME,\n\t\tD_AUTH_BEGIN,\n\t\tC_COUNTRY_CODE_WORK,\n\t\tT_PHONE_CELL,\n\t\tF_NOTES,\n\t\tT_PHONE_WORK,\n\t\tC_INDIV_REL,\n\t\tD_DOB,\n\t\tC_PHONE_AREA_HOME,\n\t\tC_PHONE_AREA_CELL,\n\t\tC_PHONE_AREA_INT,\n\t\tC_PHONE_EXT_INT,\n\t\tI_CONTRACT,\n\t\tC_AUTH_TYPE,\n\t\tN_AUTH_MID)) ~> FlatFileDestination"
    }
  }
}