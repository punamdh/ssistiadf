{
  "name" : "rest_ob_file",
  "type" : "Microsoft.DataFactory/factories/dataflows",
  "apiVersion" : "2018-06-01",
  "properties" : {
    "type" : "MappingDataFlow",
    "folder" : {
      "name" : "Elevance/PACKAGES"
    },
    "typeProperties" : {
      "sources" : [ {
        "name" : "OLEDBSource",
        "dataset" : {
          "referenceName" : "Generic_OLEDB_DS_SRC",
          "type" : "DatasetReference"
        }
      } ],
      "sinks" : [ {
        "dataset" : {
          "referenceName" : "Generic_File_TGT",
          "type" : "DatasetReference"
        },
        "name" : "FlatFileDestination"
      } ],
      "transformations" : [ {
        "name" : "DerivedColumn"
      }, {
        "name" : "DerivedColumn1"
      } ],
      "script" : "parameters{\n\tArchiveFilePath as string (),\n\tAuthFileName as string (),\n\tAuthOBFileName as string (),\n\tAuthRecCount as integer (),\n\tAuthTermFileName as string (),\n\tAuthTermRecCount as integer (),\n\tConfFileName as string (),\n\tConfOBFileName as string (),\n\tConfOBQuery as string (),\n\tConfRecCount as integer (),\n\tConfTermFileName as string (),\n\tConfTermRecCount as integer (),\n\tInboundFileLocation as string (),\n\tPWD as string (),\n\tResRecCount as integer (),\n\tResTermRecCount as integer (),\n\tRestFileName as string (),\n\tRestOBFileName as string (),\n\tRestOBQuery as string (),\n\tRestTermFileName as string (),\n\tRUNID as string (),\n\tServer as string (),\n\tToFolder as string (),\n\tUserName as string ()\n}\n\nsource(output(\n\t\tRUNID as string,\n\t\tFILETYPE as string,\n\t\tERROR_CODE as string,\n\t\tN_FIRST as string,\n\t\tN_MID_INIT as string,\n\t\tN_LAST as string,\n\t\tN_SUFFIX as string,\n\t\tI_SSN as string,\n\t\tD_DOB as timestamp,\n\t\tC_HIPAA_GENDER as string,\n\t\tF_EMPLOYEE as string,\n\t\tC_PREFIX as string,\n\t\tI_CONTRACT as string,\n\t\tI_HCID as string,\n\t\tC_INDIV_REL as string,\n\t\tC_ANT_ENT as string,\n\t\tN_FIRST_P as string,\n\t\tN_MID_INIT_P as string,\n\t\tN_LAST_P as string,\n\t\tN_SUFFIX_P as string,\n\t\tF_HONOR_RESTRICT as string,\n\t\tC_RESTRICTION as string,\n\t\tN_FIRST_RESTRICT as string,\n\t\tN_LAST_RESTRICT as string,\n\t\tD_COMPLETED as timestamp,\n\t\tN_FIRST_COMPLETED as string,\n\t\tN_LAST_COMPLETED as string,\n\t\tC_NOTIFY_METH as string,\n\t\tD_NOTIFIED as timestamp,\n\t\tN_FIRST_NOTIFIED as string,\n\t\tN_LAST_NOTIFIED as string,\n\t\tD_EFFECT_RESTRICT as timestamp,\n\t\tD_CANCEL_RESTRICT as timestamp,\n\t\tT_RESTRICT_DESCRIP as string,\n\t\tC_REQUEST_REL as string,\n\t\tC_REQUESTOR_TYPE as string,\n\t\tC_REQUEST_METHOD as string,\n\t\tD_RECEIVED as timestamp,\n\t\tFILLER as string\n\t),\n\tallowSchemaDrift: true,\n\tvalidateSchema: false,\n\tisolationLevel: 'READ_UNCOMMITTED',\n\tquery: 'User::RestOBQuery',\n\tformat: 'query',\n\tstaged:false) ~> OLEDBSource\nOLEDBSource derive(D_DOB = toString(year(D_DOB)) + right('0' + toString(month(D_DOB)), 2) + right('0' + toString(dayOfMonth(D_DOB)), 2),\n\t\tD_COMPLETED = toString(year(D_COMPLETED)) + right('0' + toString(month(D_COMPLETED)), 2) + right('0' + toString(dayOfMonth(D_COMPLETED)), 2),\n\t\tD_NOTIFIED = toString(year(D_NOTIFIED)) + right('0' + toString(month(D_NOTIFIED)), 2) + right('0' + toString(dayOfMonth(D_NOTIFIED)), 2),\n\t\tD_EFFECT_RESTRICT = toString(year(D_EFFECT_RESTRICT)) + right('0' + toString(month(D_EFFECT_RESTRICT)), 2) + right('0' + toString(dayOfMonth(D_EFFECT_RESTRICT)), 2),\n\t\tD_CANCEL_RESTRICT = toString(year(D_CANCEL_RESTRICT)) + right('0' + toString(month(D_CANCEL_RESTRICT)), 2) + right('0' + toString(dayOfMonth(D_CANCEL_RESTRICT)), 2),\n\t\tD_RECEIVED = toString(year(D_RECEIVED)) + right('0' + toString(month(D_RECEIVED)), 2) + right('0' + toString(dayOfMonth(D_RECEIVED)), 2)) ~> DerivedColumn\nDerivedColumn derive(D_EFFECT_RESTRICT = iif((D_EFFECT_RESTRICT) == '19000101', '', D_EFFECT_RESTRICT),\n\t\tD_CANCEL_RESTRICT = iif((D_CANCEL_RESTRICT) == '19000101', '', D_CANCEL_RESTRICT),\n\t\tD_RECEIVED = iif((D_RECEIVED) == '19000101', '', D_RECEIVED),\n\t\tD_DOB = iif((D_DOB) == '19000101', '', D_DOB),\n\t\tD_NOTIFIED = iif((D_NOTIFIED) == '19000101', '', D_NOTIFIED),\n\t\tD_COMPLETED = iif((D_COMPLETED) == '19000101', '', D_COMPLETED)) ~> DerivedColumn1\nDerivedColumn1 sink(input(\n\t\tD_EFFECT_RESTRICT as string,\n\t\tN_LAST_NOTIFIED as string,\n\t\tC_ANT_ENT as string,\n\t\tN_LAST_COMPLETED as string,\n\t\tF_EMPLOYEE as string,\n\t\tC_REQUEST_REL as string,\n\t\tC_PREFIX as string,\n\t\tI_SSN as string,\n\t\tD_COMPLETED as string,\n\t\tC_HIPAA_GENDER as string,\n\t\tT_RESTRICT_DESCRIP as string,\n\t\tN_LAST_P as string,\n\t\tC_RESTRICTION as string,\n\t\tC_REQUESTOR_TYPE as string,\n\t\tN_MID_INIT as string,\n\t\tN_SUFFIX_P as string,\n\t\tN_FIRST_RESTRICT as string,\n\t\tN_SUFFIX as string,\n\t\tN_LAST as string,\n\t\tN_FIRST as string,\n\t\tI_HCID as string,\n\t\tD_RECEIVED as string,\n\t\tN_FIRST_P as string,\n\t\tFILLER as string,\n\t\tC_NOTIFY_METH as string,\n\t\tC_REQUEST_METHOD as string,\n\t\tN_LAST_RESTRICT as string,\n\t\tN_MID_INIT_P as string,\n\t\tD_CANCEL_RESTRICT as string,\n\t\tC_INDIV_REL as string,\n\t\tD_DOB as string,\n\t\tN_FIRST_NOTIFIED as string,\n\t\tD_NOTIFIED as string,\n\t\tI_CONTRACT as string,\n\t\tF_HONOR_RESTRICT as string,\n\t\tN_FIRST_COMPLETED as string\n\t),\n\tallowSchemaDrift: true,\n\tvalidateSchema: false,\n\tsaveOrder: 10000,\n\tmapColumn(\n\t\tD_EFFECT_RESTRICT,\n\t\tN_LAST_NOTIFIED,\n\t\tC_ANT_ENT,\n\t\tN_LAST_COMPLETED,\n\t\tF_EMPLOYEE,\n\t\tC_REQUEST_REL,\n\t\tC_PREFIX,\n\t\tI_SSN,\n\t\tD_COMPLETED,\n\t\tC_HIPAA_GENDER,\n\t\tT_RESTRICT_DESCRIP,\n\t\tN_LAST_P,\n\t\tC_RESTRICTION,\n\t\tC_REQUESTOR_TYPE,\n\t\tN_MID_INIT,\n\t\tN_SUFFIX_P,\n\t\tN_FIRST_RESTRICT,\n\t\tN_SUFFIX,\n\t\tN_LAST,\n\t\tN_FIRST,\n\t\tI_HCID,\n\t\tD_RECEIVED,\n\t\tN_FIRST_P,\n\t\tFILLER,\n\t\tC_NOTIFY_METH,\n\t\tC_REQUEST_METHOD,\n\t\tN_LAST_RESTRICT,\n\t\tN_MID_INIT_P,\n\t\tD_CANCEL_RESTRICT,\n\t\tC_INDIV_REL,\n\t\tD_DOB,\n\t\tN_FIRST_NOTIFIED,\n\t\tD_NOTIFIED,\n\t\tI_CONTRACT,\n\t\tF_HONOR_RESTRICT,\n\t\tN_FIRST_COMPLETED)) ~> FlatFileDestination"
    }
  }
}